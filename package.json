{
  "name": "solokanban",
  "version": "8.3.0",
  "description": "Local-first, agent-native Kanban system running in browser with zero build step",
  "main": "js/main.js",
  "type": "module",
  "scripts": {
    "test": "node --test tests/tier1_unit.test.js tests/tier2_adapter.test.js tests/tier3_concurrency.test.js tests/tier4_integration.test.js tests/tier6_regression.test.js tests/tier7_sdk.test.js tests/tier8_performance.test.js tests/tier9_gap_regression.test.js",
    "test:fuzz": "node --test tests/tier0_fuzz.test.js",
    "test:python": "python3 -m unittest discover -s tests -p '*.py'",
    "test:e2e": "npx playwright test",
    "test:e2e:install": "npx playwright install --with-deps chromium",
    "test:e2e:ui": "npx playwright test --ui",
    "test:e2e:debug": "npx playwright test --debug",
    "test:all": "npm test && npm run test:fuzz && npm run test:python && npm run test:e2e",
    "serve": "node serve.js"
  },
  "keywords": [
    "kanban",
    "local-first",
    "agent-native",
    "file-system-access-api"
  ],
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  },
  "author": "",
  "license": "MIT"
}
